# Portfolio (React + Node + Prisma, Monorepo)

- apps/frontend – React (Vite)
- apps/backend – Node/Express (MVC) + Prisma + Postgres

## Scripts
- `pnpm -r dev` – run all dev servers
- `pnpm -r build` – build all
