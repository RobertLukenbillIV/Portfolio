generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// --- EXISTING ---
model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  name      String?
  role      Role     @default(ADMIN)
  createdAt DateTime @default(now())
  posts     Post[]
}

model Post {
  id        String   @id @default(uuid())
  title     String
  excerpt   String
  content   String
  coverUrl  String?
  images    Image[]
  authorId  String
  author    User     @relation(fields: [authorId], references: [id])
  published Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // NEW
  featured Boolean @default(false) // pick up to 3 for Home
}

model Image {
  id     String @id @default(uuid())
  url    String
  postId String
  post   Post   @relation(fields: [postId], references: [id])
}

// NEW: editable single pages (About, Links)
model Page {
  id        String   @id @default(uuid())
  slug      String   @unique // 'about', 'links'
  title     String
  content   String // HTML/Markdown from your editor
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())
}

// NEW: home hero + intro
model Settings {
  id          Int      @id @default(1)
  homeHeroUrl String?
  homeIntro   String?
  githubUrl   String?
  linkedinUrl String?
  
  // Hero images for different pages (JSON arrays)
  homeHeroUrls     String? // JSON array of home hero image URLs
  projectsHeroUrls String? // JSON array of projects hero image URLs
  adminHeroUrls    String? // JSON array of admin hero image URLs
  aboutHeroUrls    String? // JSON array of about hero image URLs
  
  // Image display modes for each page
  homeImageMode     String? @default("single")     // "single" or "multiple"
  projectsImageMode String? @default("single")     // "single" or "multiple"
  adminImageMode    String? @default("single")     // "single" or "multiple"
  aboutImageMode    String? @default("single")     // "single" or "multiple"
  
  updatedAt   DateTime @updatedAt
  createdAt   DateTime @default(now())
}

enum Role {
  ADMIN
  USER
}
